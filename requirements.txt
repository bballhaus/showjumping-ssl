# NOTE: torch + torchvision are intentionally NOT pinned here.
# Colab ships them preinstalled with CUDA support; `pip install torch` from
# PyPI pulls the CPU wheel and silently breaks GPU training. If you need a
# local install, use:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
ultralytics>=8.1
yt-dlp>=2024.4.9
opencv-python>=4.9
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66
einops>=0.7
av>=12.0
ipympl>=0.9
ipywidgets>=8.0
jupyter_bbox_widget>=0.6
easyocr>=1.7

